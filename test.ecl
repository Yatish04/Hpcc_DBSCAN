import datadbscan;
INTEGER add( DATA x) := BEGINC++
  // #include <iostream>
  #include<iostream>
  #include<bits/stdc++.h>
  #include<math.h>
  #option pure

  #option body
    // cout<<*x;
    return 0;
ENDC++;

// a:=(set of real)datadbscan.ds[1];
// add(datadbscan.ds[1]);
a:=DATASET([datadbscan.ds[1]],datadbscan.Layout);
a;